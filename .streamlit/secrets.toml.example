# Example Streamlit secrets for Project Samarth UI
# Copy to one of the supported locations as secrets.toml (do NOT commit real secrets):
# - ~/.streamlit/secrets.toml
# - <project>/.streamlit/secrets.toml

# The API base URL your Streamlit UI should call. For local dev:
API_BASE_URL = "http://127.0.0.1:8000"
