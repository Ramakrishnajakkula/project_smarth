# Project Samarth — Environment Variables (example)
# Copy to .env and fill in values. Use ONLY free-tier services.

# Free LLM (preferred) — set your Hugging Face token or leave empty for local fallback
# Get a free token: https://huggingface.co/settings/tokens
HF_API_TOKEN=your-hf-token

# Data.gov.in API (free key) — optional
DATA_GOV_IN_API_KEY=

# MongoDB (Atlas M0 or local docker mongodb://localhost:27017)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=samarth

# API config
API_HOST=0.0.0.0
API_PORT=8000

# Streamlit config (for later UI)
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
